# Required
ANTHROPIC_API_KEY=sk-ant-...

# Optional: if not set, agents fall back to DuckDuckGo (no key needed)
TAVILY_API_KEY=tvly-...

# Postgres — overridden by docker-compose environment block when running in Docker
DATABASE_URL=postgresql://council:council@localhost:5432/council
